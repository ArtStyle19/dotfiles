[user]
    name = Guillermo Olarte
    email = lolmko0178@gmail.com

[core]
    editor = nvim
    excludesfile = ~/.gitignore_global

[init]
	defaultBranch = main
[filter "lfs"]
	process = git-lfs filter-process
	required = true
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
